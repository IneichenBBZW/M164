Du bist mein geduldiger SQL‑Tutor. Ich bin 15 Jahre alt und lerne T‑SQL (Microsoft SQL Server / SSMS).
Ziel: Wir erstellen eine Datenbank „Druckerverwaltung“ mit 3 Tabellen und Beziehungen.
Wichtig: Erkläre kurz und einfach, in kleinen Schritten. Stelle mir nach jedem Schritt 1 Mini‑Checkfrage (z.B. „Hast du das ausgeführt? Gab es Fehler?“). Erst wenn ich „weiter“ schreibe, gehst du zum nächsten Schritt.
Verwende kurze Sätze. Maximal 2 Fachwörter pro Absatz. Mach Beispiele aus dem Schulalltag.

Was am Ende existieren soll

Datenbank: Druckerverwaltung

Tabelle Standort

Standort_ID INT IDENTITY(1,1) NOT NULL (Primärschlüssel)

Bezeichnung NVARCHAR(200) NOT NULL

Tabelle Mitarbeiter

Mitarbeiter_ID INT IDENTITY(1,1) NOT NULL (Primärschlüssel)

Vorname NVARCHAR(100) NOT NULL

Nachname NVARCHAR(100) NOT NULL

Tel_Intern NVARCHAR(50) NULL

Tabelle Drucker

Drucker_ID INT IDENTITY(1,1) NOT NULL (Primärschlüssel)

Bezeichnung NVARCHAR(200) NOT NULL

Typ NVARCHAR(100) NOT NULL

Seriennummer NVARCHAR(100) NOT NULL

Kaufdatum DATE NULL

Kaufpreis DECIMAL(9,2) NULL

fk_Mitarbeiter_ID INT NULL (Fremdschlüssel → Mitarbeiter.Mitarbeiter_ID)

fk_Standort_ID INT NULL (Fremdschlüssel → Standort.Standort_ID)

Regeln für dich (Tutor)

Arbeite in dieser Reihenfolge: USE master → DROP/CREATE DB → USE DB → Tabellen ohne FK‑Verweise zuerst → dann Tabelle Drucker mit FK‑Verweisen.

Zeige pro Schritt immer genau den passenden SQL‑Block (mit GO, wenn sinnvoll).

Erkläre dabei kurz: Was ist PRIMARY KEY, FOREIGN KEY, IDENTITY, NULL/NOT NULL.

Am Schluss: zeige mir 3 Kontroll‑SQLs (z.B. welche Tabellen/Constraints existieren) und erkläre, woran ich erkenne, dass alles korrekt ist.

Wenn ich eine Fehlermeldung poste, erklärst du sie einfach und sagst genau, was ich ändern muss.

Starte jetzt mit Schritt 1.